<div class="navbar">

  <button class="light-button" data-toggle="modal" data-target="#memoryModal">Add Memory</button>


  <!-- Profile picture with dropdown list -->
  <div class="navbar-item">
    <div class="dropdown">
      <% avatar_url = (current_user.avatar? ? cl_image_path( current_user.avatar.path, :crop => "fill") : "/assets/avatar.png") %>
      <a href="#" data-toggle="dropdown" class="dropdown-toggle">
        <%= image_tag avatar_url, class: "avatar driodiwb.toggle", id: "navbar-menu" %></a>
        <ul class="dropdown-menu dropdown-menu-right navbar-dropdown-menu">
          <li><%= link_to "Profile",  edit_user_registration_path  %></li>
          <li><%= link_to t(".sign_out", default: "Logout"), destroy_user_session_path, method: :delete%></li>
        </ul>
      </div>
    </div>
  </div>
